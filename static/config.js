// Runtime config for static hosting or subpath deployments
// If you host the frontend statically on a different origin than the backend,
// set API_BASE to the full backend base URL. Example:
// window.API_BASE = 'https://your-backend.example.com';

// Default: empty string means same-origin
window.API_BASE = window.API_BASE || '';

